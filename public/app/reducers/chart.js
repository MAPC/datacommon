import types from '../actions/types';


const defaultState = {
  cache: [],
};

export default function chart(state = defaultState, action) {

  switch(action.type) {
    default:
      return state;
  }

};
